# Silk & Spark Environment Variables
# Copy this file to .env.local and fill in the values

# Supabase Configuration
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key

# ============ AI Configuration ============

# AI Provider 选择 (可选)
# - "gemini": 直接调用 Gemini API (需要下方 API Key，仅开发使用)
# - "supabase": 通过 Supabase Edge Function 调用 (推荐，API Key 存在服务端)
# 默认: supabase (待部署后生效)
VITE_AI_PROVIDER=gemini

# Gemini API Key (仅当 VITE_AI_PROVIDER=gemini 时需要)
# ⚠️ 警告: 此 Key 会暴露在前端，仅用于开发测试
VITE_GEMINI_API_KEY=your-gemini-api-key

# ============ Stripe Payment Configuration ============

# Stripe Publishable Key (前端使用，可公开)
VITE_STRIPE_PUBLISHABLE_KEY=pk_test_your-publishable-key

# ============ Production Deployment ============
#
# 对于生产环境：
# 1. 不要设置 VITE_GEMINI_API_KEY (前端不应持有 API Key)
# 2. 在 Supabase Dashboard 或 CLI 设置:
#    supabase secrets set GEMINI_API_KEY=your-gemini-api-key
#    supabase secrets set STRIPE_SECRET_KEY=sk_live_your-secret-key
#    supabase secrets set STRIPE_WEBHOOK_SECRET=whsec_your-webhook-secret
# 3. 部署 Edge Functions:
#    supabase functions deploy ai-generate
#    supabase functions deploy create-checkout-session
#    supabase functions deploy stripe-webhook
# 4. 配置 Stripe Webhook:
#    - URL: https://your-project.supabase.co/functions/v1/stripe-webhook
#    - Events: checkout.session.completed, checkout.session.expired,
#              payment_intent.succeeded, payment_intent.payment_failed,
#              charge.refunded

